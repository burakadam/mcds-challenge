import { Layout } from '@/components/Layout';
import { Title } from '@/components/Title';

const Detail = () => {
  return (
    <Layout>
      <Title title='EDIT CAR DETAIL' />
    </Layout>
  );
};

export { Detail };
